name: Deploy BOZP portal to Pages
on:
  push:
    branches: [main]
    paths:
      - 'bozp/**'
      - '.github/workflows/pages-bozp.yml'
  workflow_dispatch:
permissions:
  contents: read
  pages: write
  id-token: write
concurrency:
  group: pages-bozp
  cancel-in-progress: true
jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Pages
        uses: actions/configure-pages@v5
        with:
          enablement: true
      - name: Upload portal
        uses: actions/upload-pages-artifact@v3
        with:
          path: './bozp'
      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4
